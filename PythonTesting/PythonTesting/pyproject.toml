[tool.poetry]
name = "pythontesting"
version = "0.1.0"
description = ""
authors = ["Quan <s3893635>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
